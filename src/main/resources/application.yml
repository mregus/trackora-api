server:
  port: 8080
  address: 0.0.0.0

spring:
  application:
    name: fleetwise-api
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: trackora-telematics
      auto-offset-reset: earliest
    producer:
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
    listener:
      missing-topics-fatal: false
  datasource:
    url: jdbc:postgresql://localhost:5432/fleetwise
    username: fleetwise
    password: fleetwise
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  logging:
    level:
      org.hibernate.SQL: INFO
      org.hibernate.orm.jdbc.bind: INFO
  flyway:
    enabled: true
    locations: classpath:db/migration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

telematics:
  kafka:
    enabled: ${TELEMATICS_KAFKA_ENABLED:false}
  geometris:
    api-key: ${GEOMETRIS_API_KEY:test-geometris-key}

jwt:
  secret: ${JWT_SECRET:trackora-local-jwt-secret-must-be-at-least-32-characters-long-123456}
  expiration-minutes: 120

management:
  endpoints:
    web:
      exposure:
        include: health,info

openai:
  enabled: ${OPENAI_ENABLED:false}
  api-key: ${OPENAI_API_KEY:dummy-key}
  base-url: https://api.openai.com/v1
  model: gpt-4o-mini

fleetwise:
  uploads-dir: ${FLEETWISE_UPLOADS_DIR:uploads}

internal:
  api-key: ${INTERNAL_API_KEY:dev-internal-key}

storage:
  provider: ${STORAGE_PROVIDER:local}

aws:
  s3:
    bucket: ${AWS_S3_BUCKET:trackora-documents}
    region: ${AWS_REGION:us-east-1}

email:
  enabled: ${EMAIL_ENABLED:true}
  provider: resend
  from: ${EMAIL_FROM:noreply@trackora.solutions}
  resend:
    api-key: ${RESEND_API_KEY:re_H9VST22W_D49Jp2pA9uVsxG4YMkSW2PYD}

app:
  frontend-url: ${APP_FRONTEND_URL:http://localhost:4200}

springdoc:
  swagger-ui:
    enabled: ${SWAGGER_ENABLED:false}
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
    display-request-duration: true
  api-docs:
    enabled: ${SWAGGER_ENABLED:false}
    path: /v3/api-docs

swagger:
  api-key: ${SWAGGER_API_KEY:dev-swagger-key}